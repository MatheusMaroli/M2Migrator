unit UMigrationType;

interface
  type

   TDataBaseType = (Varchar, Number, Long, Timestamp, Blob);
   TSQLDialectType = (Oracle, MySql, MSSQL, Postgre, SQLite);
   const MIGRATION_TABLE  : string  = 'MIGRATIONS_HISTORY';
implementation

end.
